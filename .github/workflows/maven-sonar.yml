name: Verify with Maven to SonarQube

on:
  workflow_call:
    inputs:
      SONAR_PROJECT_KEY:
        required: true
        type: string
    secrets:
      SONAR_TOKEN:
        required: true
      SONAR_HOST_URL:
        required: true

jobs:
  sonar-notice:
    if: inputs.SONAR_PROJECT_KEY == ''
    runs-on: ubuntu-latest
    steps:
      - run: echo "::notice title=SonarQube::add vars.SONAR_PROJECT_KEY to Verify with Maven to SonarQube"
  sonar:
    if: inputs.SONAR_PROJECT_KEY != ''
    runs-on: ubuntu-latest
    steps:
      - uses: dbstar-org/setup-maven@v1
        with:
          java-version: 11
          fetch-depth: 0
      - name: Cache SonarQube packages
        uses: actions/cache@v3
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
      - name: Verify with Maven to SonarQube
        env:
          GITHUB_TOKEN: ${{ github.token }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        run: |
          [ -z "$SONAR_TOKEN" ] && echo "::warning title=SonarQube::need secrets.SONAR_TOKEN" && CONTINUE='stop'
          [ -z "$SONAR_HOST_URL" ] && echo "::warning title=SonarQube::need secrets.SONAR_HOST_URL" && CONTINUE='stop'
          [ -z "$CONTINUE" ] && mvn -B -U verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=${{ inputs.SONAR_PROJECT_KEY }}
